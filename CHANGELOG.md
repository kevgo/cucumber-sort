### 0.1.1

- displays only one unorganized line per block
- prints unused regexes only when it doesn't abort early

### 0.1.0

- configuration via `cucumber-sort.json`
- `check` and `format` commands
